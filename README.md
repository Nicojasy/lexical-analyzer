# Design and software modeling of a lexical-analyzer.
Сonstruct a finite state machine for the "try-catch-finally" construct with
write capabilities inside each part of the construct, declaring a variable
of type int, as well as calling a method or calling a class method with
passing the previously declared variable.
